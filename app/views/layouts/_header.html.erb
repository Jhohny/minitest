<nav class="navbar navbar-default">
  <div class="container">
    <!--Menu "logo" -->
    <div class="navbar-header">
      <!--button for mobile view -->
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#my-navbar">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
      </button>
      <%= link_to('Minitest', root_path, class: "navbar-brand")%>
    </div>
    <!--Menu items -->
    <div class="collapse navbar-collapse" id="my-navbar">
      <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to('Brands', brands_path) %></li>
          <li><%= link_to('Products', products_path) %></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li>
            <%= link_to('<span class="glyphicon glyphicon-log-out"></span> Logout'.html_safe, destroy_user_session_path, :method => :delete) %>
            </li>
        </ul>
          
      <% else %>
      <ul class="nav navbar-nav navbar-right">
          <li>
            <%= link_to('<span class="glyphicon glyphicon-log-in"></span> Sign In'.html_safe, new_user_session_path) %>
          </li>
           <li>
            <%= link_to('<span class="glyphicon glyphicon-circle-arrow-up"></span> Sign Up'.html_safe, new_user_registration_path) %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
