	<% flash.each do |message_type, message| %>
       <div class="alert alert-info alert-dismissable fade in"><%= message %><button type="button"
    	class="close" data-dismiss="alert">×</button></div>
  	<% end %>
